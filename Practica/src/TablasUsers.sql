CREATE TABLE UNIVERSIDAD (
    ID_UNIVERSIDAD INTEGER GENERATED BY DEFAULT ON NULL AS IDENTITY START WITH 1 INCREMENT BY 1,
    NOMBRE VARCHAR(30) NOT NULL,
    UBICACION VARCHAR(50) NOT NULL,
  
    CONSTRAINT universidad_pk PRIMARY KEY(ID_UNIVERSIDAD)
    
    
)



CREATE TABLE CARRERA (
    ID_CARRERA INTEGER GENERATED BY DEFAULT ON NULL AS IDENTITY START WITH 1 INCREMENT BY 1,
    NOMBRE VARCHAR(30) NOT NULL,
    ID_UNIVERSIDAD INTEGER NOT NULL,
   
    CONSTRAINT carrera_pk PRIMARY KEY(ID_CARRERA)
    
    
)

ALTER TABLE CARRERA
ADD CONSTRAINT fk_universidad
   FOREIGN KEY (ID_UNIVERSIDAD)
   REFERENCES UNIVERSIDAD (ID_UNIVERSIDAD);